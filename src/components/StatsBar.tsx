// StatsBar is no longer rendered as a standalone section.
// Stats are embedded directly in HeroSection (see src/components/HeroSection.tsx).
// This file is kept for reference only.
export default function StatsBar() {
  return null;
}
